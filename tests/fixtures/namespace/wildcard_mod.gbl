// Test module for wildcard import
func greet(name: str): str {
    return "Hello, " + name;
}

func add(a: int, b: int): int {
    return a + b;
}

func multiply(a: int, b: int): int {
    return a * b;
}
