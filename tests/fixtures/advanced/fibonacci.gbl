import std;

func fib(n: int): int {
    if n <= 1 {
        return n;
    }
    fib(n - 1) + fib(n - 2)
}

func main() {
    for i in 0..10 {
        std::io::print(fib(i));
    }
    std::io::print("\n");
}
