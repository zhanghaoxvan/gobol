// Test alias import: `from module import ident as alias`

func main() {
    // Functions should be accessible by their aliases
    val result = calc(100, 200);
    val message = show("Aliases work!");
    
    io::println(@"Result: {result}");
    io::println(message);
}

main();
